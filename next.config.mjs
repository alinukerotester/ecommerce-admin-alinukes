/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "next-ecomercialei-alituti.s3.amazonaws.com",
    ],
  },
};

export default nextConfig;
